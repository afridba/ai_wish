import 'package:flutter/material.dart';

class BackgroundRemoverPage extends StatelessWidget {
  const BackgroundRemoverPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Background Remover')),
      body: Center(child: Text('Background Remover Functionality Here')),
    );
  }
}
